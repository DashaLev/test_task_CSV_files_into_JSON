1. Clone the git repository and install all dependencies from the JSON file.
2. Create the required folders, run npm start from the root folder "test_task_CSV_files_into_JSON".
3. Move the zip folder with CSV files to "input_directory".
4. Run npm start from the root folder "test_task_CSV_files_into_JSON folder".
5. The JSON file will be added to "output_directory".
